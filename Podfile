# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Tofu' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Tofu
  pod 'Hero'
  pod 'Firebase/Core'
  pod 'Crashlytics'
  target 'TofuTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TofuUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
